---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-manifests
  namespace: flux-system
spec:
  interval: 10m
  url: https://github.com/fluxcd/flux2
  ref:
    branch: main
  ignore: |
    # ignore all
    /*
    # include manifests
    !/manifests
