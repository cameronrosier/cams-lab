---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cameronrosier-cert
  namespace: default
spec:
  secretName: cameronrosier-tls
  issuerRef:
    name: letsencrypt-prod  # Production Let's Encrypt - trusted by browsers
    kind: ClusterIssuer
  dnsNames:
    - "cameronrosier.com"
